# Cleanup 2026-09 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove obsolete portal code, archive inactive research packages, remove the retired commercial plan, and prove the resulting product surfaces with tests and Playwright.

**Architecture:** Keep the public/API runtime and the internal cockpit, founder, operations, webhook, and attribution routes. Replace the old multi-step onboarding with `/app/confirm-profile`, make alert delivery reuse the PR6 card renderer, and keep archived research outside the Python package and test discovery. Remove the retired commercial plan while preserving unrelated internal dimensioning under neutral vocabulary.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy/Alembic, React/TypeScript/Vite, Vitest, pytest, Playwright, systemd, `kivou-deploy.sh`.

---

### Task 1: Establish branch and protected archive

**Files:**
- Create: `archive/research/spec009c/README.md`, `archive/research/README.md`, `archive/verification/README.md`, `archive/phase_a_btp/README.md`, `archive/learning/README.md`, `archive/docs/README.md`
- Move: protected Rodrigue files into `archive/research/spec009c/` and `archive/docs/`
- Modify: `.gitignore`

- [ ] Preserve the re-applied stash and add `.playwright-cli/` to `.gitignore`.
- [ ] Move archived source, fixtures, tests, and reports without deleting them.
- [ ] Document that archived directories are not package or CI inputs.

### Task 2: Remove obsolete frontend surfaces and route onboarding

**Files:**
- Delete: `frontend/src/layouts/AuthLayout.tsx`, `frontend/src/components/FormField.tsx`, `frontend/src/components/FormField.module.css`, `frontend/src/assets/Icons.tsx`, `frontend/src/cockpit/CommercialCockpit.tsx`, `frontend/src/cockpit/CommercialCockpit.module.css`, `frontend/src/cockpit/cockpit.test.tsx`, `frontend/src/pages/Onboarding.tsx`, `frontend/src/presentation/dashboard/OnboardingFlow.tsx`, `frontend/src/pages/onboarding.test.tsx`
- Create/modify: `frontend/src/pages/ConfirmProfile.tsx`, `frontend/src/pages/confirmProfile.test.tsx`, `frontend/src/App.tsx`, `frontend/src/auth/RequireAuth.tsx`, `frontend/src/signals/feed.test.tsx`, auth reference tests
- Modify: `frontend/src/api/endpoints.ts`, `frontend/src/api/types.ts`, `frontend/src/i18n/fr.ts`, `frontend/src/i18n/en.ts`

- [ ] Add a failing route assertion for `/app/confirm-profile` and no `/onboarding` route.
- [ ] Implement the three-field PR5 form using existing dashboard title/input/button primitives and existing ICP API.
- [ ] Redirect incomplete accounts to `/app/confirm-profile`, including empty defaults without a token.
- [ ] Remove the dead cockpit client/types and its i18n keys while retaining the backend route.

### Task 3: Switch alert delivery to the PR6 renderer

**Files:**
- Modify: `src/signals/alerts/job.py`, `src/signals/api/cards.py` or the shared card renderer, alert tests
- Delete: `src/signals/alerts/content.py`
- Create/modify: the real-job alert contract test

- [ ] Add a failing contract test over the job-produced `AlertMessage` and the feed card facts.
- [ ] Route job rendering through the shared PR6 card projection, including subject, text, HTML, URL, and Message-ID invariants.
- [ ] Delete the legacy renderer only after the contract passes.

### Task 4: Remove the unused filter endpoint and archive inactive packages

**Files:**
- Modify: `src/signals/api/routes_signals.py`, route architecture tests
- Move: `src/signals/research`, `src/signals/verification`, `src/signals/phase_a_btp`, `src/signals/learning`, their fixtures/tests, into matching `archive/` folders
- Modify: `pyproject.toml` only if package discovery requires an explicit exclusion

- [ ] Add an architecture test for the allowlisted route set and zero callers of `/signals/filters`.
- [ ] Remove `/signals/filters` because the current frontend uses profile data and query filters, not this endpoint.
- [ ] Move all tests importing archived packages out of pytest discovery; preserve migrations.
- [ ] Add a test that active `src/signals` cannot import `archive/`.

### Task 5: Remove the retired plan and orphan i18n safely

**Files:**
- Modify: billing catalogue/routes, frontend billing/pricing, i18n, entitlements, quotas, tests, fixtures, active docs
- Create: `frontend/scripts/count-orphan-i18n.mjs`
- Modify: `frontend/package.json` and lockfile

- [ ] Add failing assertions that the retired plan is absent from catalogue, API, UI, entitlements, fixtures, and active docs.
- [ ] Remove the retired plan and replace the public pricing column with the contact line.
- [ ] Archive or rename unrelated internal dimensioning vocabulary so the active-tree grep is empty.
- [ ] Run the AST orphan script, manually classify dynamic accesses, and remove only verified keys until at most 200 remain.
- [ ] Remove direct `@testing-library/dom` while retaining its transitive consumers.

### Task 6: Archive active documentation and stale units

**Files:**
- Move: old reports, plans, and specs into `archive/docs/` or the archive branch layout
- Modify: active documentation links and archive README files
- Host action: remove inactive `kivou-api-green.service` and `kivou-173-*` units on staging and production only after `systemctl is-active`/`is-enabled` checks

- [ ] Keep in main reports dated 2026-08-30 or newer and baselines referenced by CI.
- [ ] Keep only open-chantiers plans/specs: PR6b, PR7, and after-production.
- [ ] Record exact unit checks and removal output without exposing secrets.

### Task 7: Measure, validate, deploy, and recette

**Files:**
- Modify: `docs/reports/2026-09-08-inventaire-nettoyage.md` and create the final cleanup report

- [ ] Record before/after line counts for `src`, `frontend`, tests, and docs plus suite duration.
- [ ] Run backend/frontend tests and architecture checks.
- [ ] Deploy only through `ops/bin/kivou-deploy.sh` on staging, then production.
- [ ] Run complete Playwright on Découverte, client-3mois, and the recette account at desktop/mobile with captures.
- [ ] Commit one PR branch and report the final SHA, CI, deployment, and receipt status in exactly 15 lines.
